<?php

return [

    /*
    |--------------------------------------------------------------------------
    | AI Rate Limiting
    |--------------------------------------------------------------------------
    |
    | 1分あたりの AI API 呼び出し上限（ユーザー単位）。
    |
    */
    'rate_limit_per_minute' => (int) env('AI_RATE_LIMIT_PER_MINUTE', 3),

    /*
    |--------------------------------------------------------------------------
    | Plan Limits
    |--------------------------------------------------------------------------
    |
    | グループの料金プランごとの AI 利用上限（月次）。
    |
    */
    'plans' => [
        'free' => 3,
        'standard' => 30,
        'pro' => 50,
        'pro_plus' => 100,
    ],

    /*
    |--------------------------------------------------------------------------
    | AI Models
    |--------------------------------------------------------------------------
    |
    | 用途別の AI モデル名。
    |
    */
    'models' => [
        'ocr' => env('AI_OCR_MODEL', 'gpt-4o'), // Phase1 画像 OCR（OpenAI OCR 時のみ使用）
        'text' => env('AI_TEXT_MODEL', 'gpt-4o'), // Phase2 テキスト構造化
    ],

    /*
    |--------------------------------------------------------------------------
    | Structure Max Tokens
    |--------------------------------------------------------------------------
    |
    | Phase2 テキスト構造化（OpenAI JSON 出力）の max_tokens 上限。
    | 無制限生成を防ぎ、ParsedRecipe の件数・文字列長上限と併用する。
    |
    */
    'structure_max_tokens' => (int) env('AI_STRUCTURE_MAX_TOKENS', 16384),

];
