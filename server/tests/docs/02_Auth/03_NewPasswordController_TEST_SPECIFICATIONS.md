# NewPasswordController テストケース詳細仕様

## 概要

このドキュメントは、NewPasswordController のテストケースの詳細仕様を示します。パスワードリセット機能を検証し、システムの安定性と安全性を確保します。

## テストケース一覧表

| ID     | テスト名                                                             | 種別   | 入力条件                             | 期待される出力                                     | 該当メソッド                            |
| ------ | -------------------------------------------------------------------- | ------ | ------------------------------------ | -------------------------------------------------- | --------------------------------------- |
| 2-3-1  | 【store】 正常なパスワードリセット                                   | 正常系 | 有効なトークンとパスワードを提供     | パスワードがリセットされ、成功メッセージが返される | `NewPasswordController::store()`       |
| 2-3-2  | 【store】 バリデーションエラー（トークン未入力）                     | 異常系 | トークンが未入力                     | エラーメッセージが返される (422)                   | `NewPasswordController::store()`       |
| 2-3-3  | 【store】 バリデーションエラー（パスワード未入力）                   | 異常系 | パスワードが未入力                   | エラーメッセージが返される (422)                   | `NewPasswordRequest::rules()`           |
| 2-3-4  | 【store】 バリデーションエラー（パスワード確認未入力）               | 異常系 | パスワード確認が未入力               | エラーメッセージが返される (422)                   | `NewPasswordRequest::rules()`           |
| 2-3-5  | 【store】 バリデーションエラー（パスワード確認不一致）               | 異常系 | パスワードと確認用パスワードが不一致 | エラーメッセージが返される (422)                   | `NewPasswordRequest::rules()`           |
| 2-3-6  | 【store】 バリデーションエラー（パスワードが短すぎる）               | 異常系 | 8 文字未満のパスワードを提供         | エラーメッセージが返される (422)                   | `NewPasswordRequest::rules()`           |
| 2-3-7  | 【store】 バリデーションエラー（パスワードに英字が含まれない）       | 異常系 | 英字を含まないパスワードを提供       | エラーメッセージが返される (422)                   | `NewPasswordRequest::rules()`           |
| 2-3-8  | 【store】 バリデーションエラー（パスワードに数字が含まれない）       | 異常系 | 数字を含まないパスワードを提供       | エラーメッセージが返される (422)                   | `NewPasswordRequest::rules()`           |
| 2-3-9  | 【store】 バリデーションエラー（パスワードに記号が含まれない）       | 異常系 | 記号を含まないパスワードを提供       | エラーメッセージが返される (422)                   | `NewPasswordRequest::rules()`           |
| 2-3-10 | 【store】 無効なトークン                                             | 異常系 | 無効なトークンを提供                 | エラーメッセージが返される (404)                   | `NewPasswordController::store()`       |
| 2-3-11 | 【store】 ユーザーが存在しない                                       | 異常系 | 存在しないユーザーのトークンを提供   | エラーメッセージが返される (422)                   | `NewPasswordController::store()`       |

## テスト実行方法

### Sail 環境での実行

```bash
cd server
./vendor/bin/sail test tests/Feature/Auth/NewPasswordControllerTest.php --stop-on-failure
```
