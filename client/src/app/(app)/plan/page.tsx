import PlanPage from '@/pages/plan/PlanPage';

export const metadata = {
    title: 'Laravel - Plan',
};

const Plan = () => {
    return (
        <>
            <PlanPage />
        </>
    );
};

export default Plan;
